<div class="page-content">
    <!-- BEGIN SAMPLE PORTLET CONFIGURATION MODAL FORM-->
    <div id="portlet-config" class="modal hide">
      <div class="modal-header">
        <button data-dismiss="modal" class="close" type="button"></button>
        <h3>Widget Settings</h3>
      </div>
      <div class="modal-body"> Widget settings form goes here </div>
    </div>
    <div class="clearfix"></div>
    <div class="content">
      <ul class="breadcrumb">
          <li><p>Modules</p></li>
        <li><a href="#"><%= params[:post_type].humanize.titlecase %> Management</a></li>
        <li><a href="#" class="active">Edit <%= params[:post_type].humanize.titlecase %></a></li>
      </ul>
      <div class="page-title"> <i class="icon-custom-left"></i>
        <h3>Edit <span class="semi-bold"><%= params[:post_type].humanize.titlecase %></span></h3>
      </div>  
        
        <%= form_for @post, url: { action: "update" }, :html => { :multipart => true, :id => "form-condensed" } do |f| %>
            <%= render partial: partials[:form][:post], :locals => { :f => f, :title => "Edit Post" } %>                    
        <% end %>
    </div>
  </div>
